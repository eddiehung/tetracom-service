<% provide(:title, 'All experts') %>
<h2>
<%= User.count %> users in total, 
<%= User.count(:all, :conditions => "expeertise<>''") %> are experts
</h2>

<%= will_paginate %>

<table class="table table-striped">
<%= render @users %>
</table>

<%= will_paginate %>

<small>
	Please send suggestions for improvement or reports of inappropriate use to: <a href="mailto:thomasccp@msn.com" target="_top">thomasccp@msn.com</a>.
</small>
